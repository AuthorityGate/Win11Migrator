<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="App Selection">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml" />
                <ResourceDictionary Source="../Styles/Typography.xaml" />
                <ResourceDictionary Source="../Styles/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="#F5F0E6" Margin="48,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Select Applications" Style="{StaticResource SubtitleText}" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" Text="Choose which applications to include in the migration package."
                   Style="{StaticResource BodyText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" />

        <!-- Search + Filter Bar -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearch" Grid.Column="0" Style="{StaticResource SearchTextBox}"
                     Margin="0,0,12,0" />

            <Button x:Name="btnSelectAll" Grid.Column="1" Content="Select All"
                    Style="{StaticResource SecondaryButton}" Padding="12,6" MinWidth="80"
                    Margin="0,0,8,0" FontSize="12" />
            <Button x:Name="btnDeselectAll" Grid.Column="2" Content="Deselect All"
                    Style="{StaticResource SecondaryButton}" Padding="12,6" MinWidth="80"
                    Margin="0,0,8,0" FontSize="12" />
            <TextBlock x:Name="txtSelectedCount" Grid.Column="3" VerticalAlignment="Center"
                       Style="{StaticResource CaptionText}" />
        </Grid>

        <!-- App List -->
        <Border Grid.Row="3" Style="{StaticResource CardPanel}" Padding="0" Margin="0">
            <ListView x:Name="listApps" BorderThickness="0" Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="" Width="40">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Selected}"
                                              Style="{StaticResource MigratorCheckBox}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Application" Width="280" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Version" Width="120" DisplayMemberBinding="{Binding Version}" />
                        <GridViewColumn Header="Install Method" Width="130" DisplayMemberBinding="{Binding InstallMethod}" />
                        <GridViewColumn Header="Source" Width="100" DisplayMemberBinding="{Binding Source}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Summary -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock x:Name="txtSummary" Style="{StaticResource CaptionText}" />
        </StackPanel>
    </Grid>
</Page>
