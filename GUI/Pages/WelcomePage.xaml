<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Welcome">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml" />
                <ResourceDictionary Source="../Styles/Typography.xaml" />
                <ResourceDictionary Source="../Styles/Controls.xaml" />
                <ResourceDictionary Source="../Styles/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="#F5F0E6">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="700">

            <!-- Title -->
            <TextBlock Text="AuthorityGate User Migrator" Style="{StaticResource TitleText}"
                       HorizontalAlignment="Center" Margin="0,0,0,8" />
            <TextBlock Text="Transfer your apps, files, and settings to your new Windows 11 PC"
                       Style="{StaticResource BodyText}" HorizontalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,48" />

            <!-- Mode Selection Cards -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Export Card -->
                <Border x:Name="cardExport" Grid.Column="0" Style="{StaticResource ModeCard}">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="M9,16V10H5L12,3L19,10H15V16H9M5,20V18H19V20H5Z"
                              Fill="{StaticResource PrimaryBrush}" Width="48" Height="48"
                              Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,16" />
                        <TextBlock Text="Export" Style="{StaticResource SubtitleText}"
                                   HorizontalAlignment="Center" Margin="0,0,0,8" />
                        <TextBlock Text="Scan this PC and package your apps, files, and settings for transfer"
                                   Style="{StaticResource BodyText}" TextAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}" MaxWidth="250" />
                    </StackPanel>
                </Border>

                <!-- Import Card -->
                <Border x:Name="cardImport" Grid.Column="1" Style="{StaticResource ModeCard}">
                    <StackPanel HorizontalAlignment="Center">
                        <Path Data="M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z"
                              Fill="{StaticResource AccentBrush}" Width="48" Height="48"
                              Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,16" />
                        <TextBlock Text="Import" Style="{StaticResource SubtitleText}"
                                   HorizontalAlignment="Center" Margin="0,0,0,8" />
                        <TextBlock Text="Restore apps, files, and settings from a migration package"
                                   Style="{StaticResource BodyText}" TextAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}" MaxWidth="250" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
