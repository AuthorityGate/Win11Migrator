<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Exporting">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml" />
                <ResourceDictionary Source="../Styles/Typography.xaml" />
                <ResourceDictionary Source="../Styles/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="#F5F0E6" Margin="48,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtExportTitle" Text="Exporting Migration Package..."
                   Style="{StaticResource SubtitleText}" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" x:Name="txtExportPhase" Text="Preparing..."
                   Style="{StaticResource BodyText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24" />

        <!-- Overall progress -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="progressExport" Grid.Column="0" Style="{StaticResource MigratorProgressBar}"
                         Value="0" Maximum="100" Margin="0,0,12,0" />
            <TextBlock x:Name="txtExportPercent" Grid.Column="1" Text="0%" Style="{StaticResource BodyText}" Width="45" />
        </Grid>

        <!-- Current item -->
        <TextBlock Grid.Row="3" x:Name="txtCurrentItem" Text=""
                   Style="{StaticResource CaptionText}" Margin="0,0,0,16" TextTrimming="CharacterEllipsis" />

        <!-- Log viewer -->
        <Border Grid.Row="4" Style="{StaticResource CardPanel}" Padding="0" Margin="0">
            <TextBox x:Name="txtExportLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="11" Background="Transparent" BorderThickness="0"
                     Padding="12" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Page>
