<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Primary Colors (AuthorityGate Corporate) -->
    <Color x:Key="PrimaryColor">#4A2C17</Color>          <!-- Dark Chocolate - header/primary -->
    <Color x:Key="PrimaryDarkColor">#3C2415</Color>      <!-- Espresso - hover on primary -->
    <Color x:Key="PrimaryLightColor">#5C3D2E</Color>     <!-- Burnt Umber - lighter variant -->
    <Color x:Key="AccentColor">#C9A227</Color>           <!-- Gold - dominant accent -->

    <!-- Status Colors -->
    <Color x:Key="SuccessColor">#107C10</Color>
    <Color x:Key="WarningColor">#D4A84B</Color>          <!-- Amber -->
    <Color x:Key="ErrorColor">#9B2335</Color>            <!-- Burgundy (sparse) -->
    <Color x:Key="InfoColor">#C17F59</Color>             <!-- Copper -->

    <!-- Neutral Colors -->
    <Color x:Key="BackgroundColor">#F5F0E6</Color>       <!-- Cream background -->
    <Color x:Key="SurfaceColor">#FFFDF8</Color>          <!-- Warm white -->
    <Color x:Key="BorderColor">#E8DCC8</Color>           <!-- Beige/Cream -->
    <Color x:Key="TextPrimaryColor">#3D3D3D</Color>      <!-- Warm charcoal -->
    <Color x:Key="TextSecondaryColor">#8A8580</Color>    <!-- Warm gray -->
    <Color x:Key="TextDisabledColor">#B5B0AA</Color>     <!-- Light warm gray -->
    <Color x:Key="HoverColor">#E8DCC8</Color>            <!-- Beige hover -->
    <Color x:Key="SelectedColor">#F0E8D4</Color>         <!-- Warm selected -->

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}" />
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}" />

    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="TextDisabledBrush" Color="{StaticResource TextDisabledColor}" />
    <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}" />
    <SolidColorBrush x:Key="SelectedBrush" Color="{StaticResource SelectedColor}" />

</ResourceDictionary>
