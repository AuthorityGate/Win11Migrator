<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AuthorityGate User Migrator" Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F0E6">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Colors.xaml" />
                <ResourceDictionary Source="Styles/Typography.xaml" />
                <ResourceDictionary Source="Styles/Controls.xaml" />
                <ResourceDictionary Source="Styles/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="AuthorityGate User Migrator" FontSize="20" FontWeight="SemiBold"
                           Foreground="White" VerticalAlignment="Center"
                           FontFamily="{StaticResource PrimaryFont}" />

                <TextBlock x:Name="txtStepIndicator" Grid.Column="2"
                           Text="Step 1 of 6" FontSize="14" Foreground="#D4A574"
                           VerticalAlignment="Center"
                           FontFamily="{StaticResource PrimaryFont}" />
            </Grid>
        </Border>

        <!-- Content Frame -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"
                   Margin="0" />
        </ScrollViewer>

        <!-- Footer Navigation -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="btnCancel" Grid.Column="0" Content="Cancel"
                        Style="{StaticResource SecondaryButton}" />

                <Button x:Name="btnBack" Grid.Column="2" Content="Back"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,12,0" />

                <Button x:Name="btnNext" Grid.Column="3" Content="Next"
                        Style="{StaticResource PrimaryButton}" />
            </Grid>
        </Border>
    </Grid>
</Window>
